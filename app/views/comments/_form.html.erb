<%= form_for([@article, @article.comments.build]) do |f| %>
	<p>
		<%= f.label :user_id %><br>
		<%= f.text_area :user_id %>
	</p>
	<p>
		<%= f.label :text %><br>
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>